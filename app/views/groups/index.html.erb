<%= render 'users/user_show', users: @users %>


<!--本の新規投稿-->
<%= render 'books/book_new', book: @book %>

<!--投稿している本の情報---->
<div class="col-md-8 offset-md-1">
<h2>Groups</h2><%= link_to 'ユーザーページへ', current_user %>
<table class="table table-hover table-inverse">
<thred>
 <tr>
  <th></th>
  <th>グループ名</th>
  <th>紹介文</th>
  <th colspan="3"></th>
  </tr>
</thred>

 <tbody>
<tr>
<% @group_lists.each do |list| %>
<td><%= attachment_image_tag list, :image,format:'jpeg',size: "50x50", fallback: "no_image.jpg" %></td>
<td><%= link_to "/groups/#{list.id}" do %><%= list.name %><% end %></td>
<td><%= list.introduction %></td>
<td><%= link_to "Edit",edit_group_path(list.id) %></td>
</tr>
<% end %>

</tbody>

</table>
</div>


 